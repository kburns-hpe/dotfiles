#!/bin/zsh

# Doing Aliases
# https://github.com/ttscoff/doing

alias dn="doing now"
alias ddone="doing done"
alias dl="doing later"
alias dnext="doing done; doing now"
alias dtt="doing today --totals"
